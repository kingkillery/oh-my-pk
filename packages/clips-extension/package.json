{
	"name": "@pk-nerdsaver-ai/clips-extension",
	"version": "16.4.12",
	"description": "Oh My PK extension: feed Clips (gopk.xyz) screen recordings to agents",
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"exports": {
		".": {
			"import": "./src/index.ts",
			"types": "./src/index.ts"
		}
	},
	"scripts": {
		"check": "biome check . && bun run check:types",
		"check:types": "tsgo -p tsconfig.json --noEmit",
		"lint": "biome lint .",
		"fix": "biome check --write --unsafe .",
		"fmt": "biome format --write ."
	},
	"dependencies": {
		"@pk-nerdsaver-ai/pi-ai": "workspace:*",
		"@pk-nerdsaver-ai/pi-coding-agent": "workspace:*"
	},
	"omp": {
		"extensions": [
			"./src/index.ts"
		]
	},
	"files": [
		"src",
		"CHANGELOG.md"
	],
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^6.0.3"
	},
	"peerDependencies": {
		"@pk-nerdsaver-ai/pi-coding-agent": "^13"
	}
}
