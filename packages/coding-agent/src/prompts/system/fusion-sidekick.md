## Sidekick (cost mode)
`{{sidekickId}}` is the existing registered sidekick configured for `{{sidekickModel}}`. Minimize your own actions: keep planning, design, ambiguity resolution, root-cause debugging, and final review; send settled mechanical work with `{{hubToolRef}}` using `op: "send"`, `to: "{{sidekickId}}"`, and a self-contained `message`. Do not spawn another task agent with this name.
Assignments must be narrow, self-contained, and include acceptance criteria.
{{#if fusionEscalate}}Cheap-first, but escalate the hard parts to your own reasoning.{{/if}}
