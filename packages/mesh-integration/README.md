# LocalMesh first-slice acceptance

This package proves a deterministic, harmless in-memory lifecycle across the
new mesh boundaries: signed task, safe placement, scheduler lease, bound OMPK
execution adapter, content-addressed artifact, evidence/receipt, and durable
outbox projection. It is deliberately not a replacement for the planned
Linux-worker, SQLite, or Nostr deployment gates.
