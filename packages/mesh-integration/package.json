{
	"name": "@pk-nerdsaver-ai/mesh-integration",
	"version": "0.1.0",
	"description": "Composed first-slice acceptance tests for LocalMesh",
	"private": true,
	"type": "module",
	"scripts": {
		"check": "biome check . && bun run check:types",
		"check:types": "tsgo -p tsconfig.json --noEmit",
		"lint": "biome lint .",
		"test": "bun test --parallel"
	},
	"devDependencies": {
		"@types/bun": "catalog:"
	},
	"engines": {
		"bun": ">=1.3.14"
	}
}
