# Add one of these to .ompk/config.yml or ~/.ompk/agent/config.yml.

# Direct file mode:
extensions:
  - /absolute/path/to/llm-router-agent-tooluse/dist/extension.js

# Directory mode also works because package.json contains:
#   "omp": { "extensions": ["./dist/extension.js"] }
#   "pi":  { "extensions": ["./dist/extension.js"] }
# extensions:
#   - /absolute/path/to/llm-router-agent-tooluse
