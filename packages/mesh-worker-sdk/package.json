{
	"name": "@pk-nerdsaver-ai/mesh-worker-sdk",
	"version": "0.1.0",
	"description": "Transport-neutral worker protocol and OMPK execution adapter seam",
	"private": true,
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"check": "biome check . && bun run check:types",
		"check:types": "tsgo -p tsconfig.json --noEmit",
		"lint": "biome lint .",
		"test": "bun test --parallel=4 test"
	},
	"dependencies": {
		"@pk-nerdsaver-ai/mesh-contracts": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "catalog:"
	},
	"engines": {
		"bun": ">=1.3.14"
	}
}
